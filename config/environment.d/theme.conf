# Make Qt6 apps follow the GTK theme (libqgtk3 plugin) → Chrome Gray dark.
# Loaded by systemd --user at login and imported into the Sway session.
QT_QPA_PLATFORMTHEME=gtk3

# Ensure Qt honors per-monitor scaling on Wayland (harmless default).
QT_AUTO_SCREEN_SCALE_FACTOR=1
